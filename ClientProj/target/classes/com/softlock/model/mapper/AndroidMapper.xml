<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.softlock.model.AndroidImpl">

	<insert id ="memjoinAction" parameterType="String">
		INSERT INTO member
		(MEM_IDX, MEM_ID, MEM_PW, MEM_NAME, MEM_BIRTH, MEM_PHONE, MEM_GENDER, MEM_EMAIL, MEM_AUTH) 
		VALUES (seq_member.nextval,#{param1},#{param2},#{param3},#{param4},#{param5},#{param6},#{param7},#{param8})
	</insert>
	
	<select id="searchHp" resultType="com.softlock.model.HospitalDTO" parameterType="String">
		select * from hospital where hp_name LIKE '%'||#{param4}||'%' and hp_type=#{param1} and hp_night=#{param2} and hp_wkend=#{param3}
	</select>
	
</mapper>